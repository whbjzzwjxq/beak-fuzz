"""
Three-pass Nexus patch pipeline.

The tracked Nexus benchmark snapshot currently builds against the upstream source layout without
local source rewrites, but the pass package keeps install semantics uniform with the other zkVMs.
"""

