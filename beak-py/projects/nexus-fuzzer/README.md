# nexus-fuzzer

Installer entrypoint for materializing a Nexus zkVM snapshot into `beak-py/out/`.

The default install root is repo-stable now. Prefer `make nexus-install` from `beak-fuzz/`, or run
the CLI from `beak-py/` when you need a direct install.
