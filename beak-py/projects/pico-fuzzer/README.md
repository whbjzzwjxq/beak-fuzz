# pico-fuzzer

Python-side installer package for Pico integration in `beak-py`.

## Commands

- `pico-fuzzer install`

The install command materializes a Pico snapshot at:
`out/pico-<commit>/pico-src`.
