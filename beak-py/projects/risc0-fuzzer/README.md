# RISC0 Fuzzer Installer

Materializes a pinned RISC0 snapshot under `beak-py/out/risc0-<commit>/risc0-src`.

The default install root is repo-stable now. Prefer `make risc0-install` from `beak-fuzz/`, or run
the CLI from `beak-py/` when you need a direct install.
